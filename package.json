{
  "name": "pdfsvex",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm run --parallel --stream build",
    "lint": "pnpm run --parallel --stream lint",
    "preinstall": "npx only-allow pnpm",
    "prettier": "pnpm run --parallel --stream prettier",
    "publish": "pnpm publish --filter \"@pdfsvex/*\" --access public",
    "version": "pnpx changeset version && pnpm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.16.0",
    "@changesets/changelog-github": "^0.4.0",
    "eslint": "^7.5.0",
    "prettier": "^2.3.0"
  }
}